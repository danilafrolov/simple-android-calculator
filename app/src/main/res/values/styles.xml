<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CalculatorButton">
        <item name="android:layout_width">75dp</item>
        <item name="android:layout_height">75dp</item>
        <item name="android:textSize">24sp</item>

    </style>

    <style name="CalculatorButton.Action">
        <item name="android:textColor">@color/color_button_action_text</item>
        <item name="android:background">@drawable/button_bg_round_action</item>
        <item name="android:textSize">36sp</item>
    </style>

    <style name="CalculatorButton.Digit">
        <item name="android:background">@drawable/button_bg_round_digit</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="CalculatorButton.Result">
        <item name="android:background">@drawable/button_bg_round_result</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">36sp</item>
    </style>
</resources>
